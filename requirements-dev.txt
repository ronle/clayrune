# Dev / test dependencies — NOT required for a normal Clayrune install.
# Install with: pip install -r requirements-dev.txt
# Run tests with: pytest         (main app, ./tests)
#                 pytest control_plane/tests   (control plane)
-r requirements.txt

pytest>=8.0
httpx>=0.27          # used by control_plane/tests fixtures (MockTransport)
